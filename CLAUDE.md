# 项目约定（仓库级）

## Git commit
- **不要在 commit message 里添加任何 `Co-Authored-By` 尾注**（尤其是 `Co-Authored-By: Claude ...`）。
- commit 只显示 author = `nuke`（仓库 `user.name=nuke` / `user.email=mikenornuke.yu@gmail.com`，已正确，无需改动）。
- 不要在 commit message 里添加 Claude / Generated with Claude Code 之类的署名或链接。
- 保持 commit message 干净：只描述本次改动本身。

> 后端专属约定见 [backend/CLAUDE.md](backend/CLAUDE.md)。
