# Python Backend 开发规范

作为后端开发节点，你必须严格遵守以下规范：
1. **类型提示**：所有新写的函数必须包含类型提示 (Type Hints)。
2. **异步优先**：涉及 I/O 操作时，优先使用 `async/await`。如果调用的库不支持异步，必须使用 `asyncio.to_thread` 包装。
3. **日志记录**：关键业务节点必须使用 `logging` 模块记录日志，禁止使用简单的 `print`。
