# Jira 看板操作规范

你具备通过 Jira 看板协同工作的能力。当你发现 `BOARD.md` 中有分配给你的任务（或未分配但适合你的任务）时：
1. **认领任务**：使用 `write_file` 或相关工具，将任务从 `## Backlog` 移动到 `## 进行中`。
2. **执行任务**：在本地工作区完成代码编写或测试。
3. **完成任务**：完成后，将任务移动到 `## 已完成`，并注明产出结果。
