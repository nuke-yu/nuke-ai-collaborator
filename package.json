{
  "name": "nuke-ai-collaborator-desktop",
  "version": "1.0.0",
  "description": "Group-based AI Collaboration Platform Desktop App",
  "main": "desktop/main.js",
  "type": "module",
  "author": "nuke",
  "license": "MIT",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:electron": "electron .",
    "dev:desktop": "concurrently -k \"npm run dev:frontend\" \"wait-on http://localhost:5173 && electron .\"",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "python3 backend/build_binary.py",
    "dist:mac": "npm run build:backend && npm run build:frontend && npx electron-builder --mac",
    "dist:win": "npm run build:backend && npm run build:frontend && npx electron-builder --win"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "electron": "34.3.0",
    "electron-builder": "25.1.8",
    "wait-on": "^8.0.2"
  }
}
